### Out of 14 points

### Enforced by the checkpoint test suite (suite present and passing = full points):

1 - Integration test exists

1 - Integration test infrastructure exists

3 - Unit tests (1 pt per API)

3 - Empty implementations (1 pt per API)

-1 - Any checks failing


### Other:
1 - All tests run and fail (and are therefore excluded in build.gradle)

1 - Good commit messages for merge commits: for this checkpoint, anything other than the autogenerated ones are fine but can be flagged for not being descriptive

2 - Gave a code review: requirements increase over time, here a single comment (vs comments on specific lines) is only ok if it's very thorough

2 - Received a code review: should be sending out between 50-200 lines of code

### Important Notes:

Keep an eye out for folks trying to game the checkpoint tests by adding unused variables to their tests. If they do, they don't lose points, but they do have to actually fix things before the next checkpoint